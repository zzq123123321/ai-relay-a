# AI Relay A端架构

## 部署角色

A端运行于 ChatGPT 网页所在电脑，只负责操作 ChatGPT 网页和 Windows 共享剪贴板。

A端不识别、不选择也不标记B端使用 OpenCode、Reasonix 或其他执行软件。

## 协议角色

### A端写入执行任务

```text
SOURCE: CHATGPT
TARGET: EXECUTOR
TYPE: TASK
```

### A端接收执行结果

```text
SOURCE: EXECUTOR
TARGET: CHATGPT
TYPE: RESPONSE
```

迁移期可读取 `SOURCE: OPENCODE` 和 `SOURCE: REASONIX` 的旧响应，但新消息只写入角色标识。

## 标定模型

A端只有一个 UI Surface：`chatgpt_web`。

A端控制窗口始终置顶，便于观察运行状态和随时停止；该设置不得改变或缩放 Chrome 窗口。

正式运行时双击 `start_silent.vbs`，由 `pythonw.exe` 启动，不显示命令窗口。不要使用 `python app.py` 作为日常启动方式。

```text
chatgpt_web
├── input_box
├── trigger
├── bottom
└── copy
```

标定对象是被操作的 ChatGPT 网页，不是消息来源 Agent。Reasonix 的 UIA 自检和执行属于B端。

回复完成后，A端在至少10秒内持续通过模板检测“到底”按钮，只在检测成功时点击识别到的实际坐标，不允许盲点；10秒超时表示回复较短、回复结尾未到页面最下方，此时把鼠标定位到 ChatGPT 网页标定区域并将鼠标滚轮向上滚动一个刻度，使页面内容向下移动，然后继续检测“到底”按钮。检测到并点击到底后才进入“复制回复”按钮检测；如果复制按钮尚未出现，每轮同样将鼠标滚轮向上滚动一个刻度，再继续检测，直到找到复制按钮。

每次滚动前必须确认目标坐标属于 Chrome，并将 Chrome 激活到前台；滚动后写入包含方向和坐标的诊断日志。坐标被A端窗口遮挡或 Chrome 无法激活时必须明确报错，不得记录虚假的滚动成功。

点击“复制回复”按钮后必须检查 Windows 剪贴板是否发生变化。未变化时不得结束或报最终失败，必须重新识别按钮的当前坐标并再次点击，直到成功复制。

所有界面操作步骤之间必须保留延迟，默认 `interaction_step_delay=0.5` 秒，包括识别后、移动鼠标后、点击后、滚动后、检查剪贴板和进入重试前，避免网页动画或焦点切换导致操作丢失。

A端把执行结果粘贴到 ChatGPT 输入框并按 Enter 后立即开始持续检测“回复完成图标”，不设置检测前等待。图标只在标定位置附近的小范围内以至少0.9阈值识别，避免把页面其他相似图标误判为未变化。若持续10秒未捕获任何变化，A端重新点击输入框并按 Enter。只有图标先变化、之后重新出现，才可判定本轮回复完成。

顺序标定时，当前步骤保存后点击“下一目标”必须立即结束该步骤并自动进入下一项。每个标定项也必须支持“单独重标”，修正一项时不得清空或覆盖其他已完成项。

## 路由约束

- A端只消费 `TARGET: CHATGPT + TYPE: RESPONSE`。
- `TARGET: EXECUTOR` 是A端自己写出的任务，A端不重复消费。
- 未知目标不得回退到任何默认 Agent。
- 已消费响应ID会持久化去重。
- `TARGET: AI_RELAY + TYPE: DIAGNOSTIC` 只执行只读诊断。

## 自动完成规则

- 网页回复去除首尾空白后，只有完全等于 `AI_RELAY_COMPLETE` 才表示开发完成并停止向B端传递。
- 正文中仅提到结束标记不得触发停止，避免误判。
- 普通回复不受轮次限制，始终继续包装成下一轮 TASK。
- `MAX_ROUNDS` 字段仅为兼容B端现有协议而保留；达到字段值时自动扩展，不作为停止条件。
